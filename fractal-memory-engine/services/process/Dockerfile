FROM python:3.11-slim

WORKDIR /app

COPY services/process/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY shared /app/shared
COPY services/process/app.py .

ENV PYTHONPATH=/app

EXPOSE 8002

CMD ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8002"]
