# NOTE: Use sealed-secrets or external secret management in production
# This is a template - DO NOT commit actual secrets
apiVersion: v1
kind: Secret
metadata:
  name: fractal-memory-secrets
  namespace: fractal-memory
type: Opaque
stringData:
  # Google Cloud
  GOOGLE_APPLICATION_CREDENTIALS: "/secrets/gcp/credentials.json"

  # LLM APIs
  OPENAI_API_KEY: "CHANGE_ME_IN_PRODUCTION"
  ANTHROPIC_API_KEY: "CHANGE_ME_IN_PRODUCTION"

  # Cloudflare KV
  CF_ACCOUNT_ID: "CHANGE_ME_IN_PRODUCTION"
  CF_KV_NAMESPACE: "CHANGE_ME_IN_PRODUCTION"
  CF_API_TOKEN: "CHANGE_ME_IN_PRODUCTION"
