# 🌑 ORBOS / eKo System

> **"From intent to empire. Everybody Eats."**

The personal AI production system that generates revenue, builds products, and scales infinitely.

## 🚀 Quick Start

```bash
# Boot eKo
python eko-launch.py

# Or run a glyph command
python eko-launch.py "BUILD DEPLOY"
```

## 📁 Architecture

```
/ORBOS
│
├── /core-builder        ← Private Builder Engine
│   ├── ingest/          ← Repo/folder ingestion
│   ├── classify/        ← Collision detection, canonical IDs
│   ├── resolve/         ← Deduplication
│   ├── memory/          ← Persistent snapshot DB
│   └── emit/            ← Structured output
│
├── /orb-core            ← Private intelligence substrate
│   ├── agents/          ← Live AI agents
│   ├── orchestration/   ← 47 fractal nodes
│   ├── debate/          ← Node-to-node arbitration
│   ├── memory/          ← Persistent memory
│   ├── policies/        ← Moral vector / constraints
│   └── revenue-engine/  ← Crypto rails
│
├── /0r8-ai              ← Public AI interface
│   ├── ui/
│   ├── api/
│   └── adapters/
│
├── /eko-vision          ← Brand, culture, commerce
│   ├── media/
│   ├── merch/
│   └── community/
│
├── /glyph-lang          ← Voice-first DSL
│   ├── grammar/
│   ├── symbols/
│   ├── macros/
│   └── compiler/
│
└── /archives            ← Raw ingested data
```

## ✨ Glyph Commands

| Glyph | Symbol | Action |
|-------|--------|--------|
| BUILD | 🔨 | Emit unified monorepo |
| INGEST | 📥 | Ingest files/data |
| CANON | 📋 | Canonicalize modules |
| DEPLOY | 🚀 | Deploy to production |
| AWAKEN | ⚡ | Awaken agents |
| SPAWN | ✨ | Spawn fractal nodes |
| SWARM | 🐝 | Activate swarm mode |
| OWL | 🦉 | Switch to Owl spirit |
| FOX | 🦊 | Switch to Fox spirit |
| FULLBUILD | 🏗️ | Complete build pipeline |

## 💰 Revenue Engine

- **Proof-of-Attribution**: Every action tracked with SHA-256 hash
- **Automated Splits**: 40% Architect, 25% Agents, 20% Treasury, 10% Contributors, 5% Reserve
- **Crypto Rails**: Transparent, auditable revenue flow

## 🔒 Human Safety Switch (HSS)

```bash
# Deploy HSS
sudo bash ../0r8-starter/deploy/hss-deploy.sh

# Open gate (human present)
sudo orb-human-auth enable 300 "deployment"

# Check guard
orb-guard modify_boot || exit 1
```

## 🧠 47 Fractal Nodes

Self-scaling, recursive AI runtime:
- Perpetual execution
- Energy wave optimization
- Recursive spawning up to depth 7
- Task parallel execution

## 📊 Status

```bash
python eko-launch.py
> status
```

---

**Architect**: JB
**Spirit**: 🦉 Owl + 🦊 Fox
**Philosophy**: Love, Loyalty, Honor. Everybody Eats. We Are One.
